import React from 'react';
import Authform from "@/Components/Authform";

const Page = () => {
    return (
        <div>
            return <Authform type="sign-in"/>
        </div>
    );
};

export default Page;
